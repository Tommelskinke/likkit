name: CI test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repo
      uses: actions/checkout@v3

    - name: Setup Node.js environment
      uses: actions/setup-node@v4.0.0

    - name: Install dependencies server
      run: npm install
      working-directory: whiteboard-service-2/server

    - name: Install dependencies client
      run: npm install
      working-directory: whiteboard-service-2/client


  test:
    name: Tests
    runs-on: ubuntu-latest

    steps:
    - name: Check out repo
      uses: actions/checkout@v3

    - name: Setup Node.js environment
      uses: actions/setup-node@v4.0.0
      
    - name: Prettier
      run: ./node_modules/.bin/prettier --check .
      working-directory: whiteboard-service-2/server

    - name: TypeScript
      run: ./node_modules/.bin/tsc --noEmit --skipLibCheck
      working-directory: whiteboard-service-2/server
      
    - name: Run Tests
      run: npm test
      working-directory: whiteboard-service-2/server
